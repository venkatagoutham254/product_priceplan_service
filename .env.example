# 🔒 SECURITY CONFIGURATION - Environment Variables
# Copy this file to .env and set your actual values
# NEVER commit .env file to version control!

# 🗄️ Database Configuration
DB_URL=jdbc:postgresql://your-database-host:5432/your-database-name
DB_USERNAME=your-database-username
DB_PASSWORD=your-secure-database-password

# 🔐 Encryption Configuration
JASYPT_ENCRYPTOR_PASSWORD=your-very-secure-encryption-password

# 🌐 Redis Configuration (if using Redis)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your-redis-password

# 🔑 JWT Configuration (if needed)
JWT_SECRET=your-jwt-secret-key

# 📧 External Service URLs (if needed to be secured)
CUSTOMER_SERVICE_URL=http://your-customer-service-url
BILLABLE_METRICS_SERVICE_URL=http://your-billable-metrics-service-url

# 🏭 Production Environment Variables
SPRING_PROFILES_ACTIVE=postgres
SERVER_PORT=8080

# 📊 Monitoring (if using external services)
# MONITORING_API_KEY=your-monitoring-api-key
# LOGGING_ENDPOINT=your-logging-endpoint

# 🚀 Deployment Configuration
# CORS_ALLOWED_ORIGINS=https://your-frontend-domain.com
# CORS_ALLOWED_METHODS=GET,POST,PUT,PATCH,DELETE,OPTIONS
