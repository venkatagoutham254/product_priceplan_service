# JWT Configuration - Use the same secret for both local and Docker
JWT_SECRET=change-me-please-change-me-32-bytes-min

# Database Configuration
DB_PASSWORD=P4ssword!
DB_USERNAME=root

# Redis Configuration  
REDIS_HOST=redis
REDIS_PORT=6379
